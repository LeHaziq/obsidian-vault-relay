{
  "name": "vault-relay",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @vault-relay/protocol && npm run build --workspace vault-relay-obsidian && npm run build --workspace @vault-relay/oauth-relay",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
